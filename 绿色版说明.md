# 🌟 绿色版英语学习App使用说明

## 📦 包含文件（复制整个 `english_app` 文件夹即可）

```
english_app/
├── start.bat          ← 双击启动（每次开机后双击一次）
├── 启动App.bat        ← 中文版启动
├── 自动启动安装.bat    ← 安装开机自启动（只需运行一次）
├── 卸载自启动.bat     ← 取消开机自启动
├── start_app.py       ← 启动脚本
├── backend/           ← 后端程序
└── frontend/          ← 前端页面
```

## 🚀 第一次使用（任意电脑）

### 步骤1：复制文件夹
把整个 `english_app` 文件夹复制到新电脑（U盘、网盘都行）

### 步骤2：安装 Python（如果电脑没有）
- 打开 https://www.python.org/downloads/
- 下载 Python 3.x 安装包
- **安装时务必勾选 "Add Python to PATH"** ✅

### 步骤3：安装依赖（首次）
在新电脑上，双击运行：
```
安装依赖.bat
```

### 步骤4：设置开机自启动（可选）
双击运行：
```
自动启动安装.bat
```

以后每次开机，应用会自动启动，无需任何操作！

## 📱 Pad/手机使用

**确保 Pad/手机和电脑连同一个 WiFi**

浏览器打开：
```
http://10.255.100.251:8080
```

（如 IP 变化，查看 `我的IP.bat`）

## 🔧 常用脚本

| 脚本 | 作用 |
|---|---|
| `start.bat` | 启动应用（手动） |
| `停止服务.bat` | 停止应用 |
| `我的IP.bat` | 查看本机 IP 地址 |
| `自动启动安装.bat` | 设置开机自启 |
| `卸载自启动.bat` | 取消开机自启 |

## 💻 适用场景

✅ 家里电脑 → Pad 学习
✅ 公司电脑 → 给孩子学习
✅ 笔记本带到任何地方 → 随时学
✅ 朋友家临时用 → U盘拷过去

## ❓ 常见问题

**Q: 换了电脑，IP 会变吗？**
A: 会变。每次启动看 `我的IP.bat` 的输出。

**Q: Pad 搜不到怎么办？**
A: 
1. 电脑和 Pad 必须在同一 WiFi
2. 关闭电脑防火墙（设置 → 网络 → 防火墙 → 关闭）
3. 在 Pad 浏览器输入电脑 IP

**Q: 电脑关机后还能用吗？**
A: 不能。电脑必须开机。

**Q: 数据存在哪里？**
A: 当前在内存，重启后清空。如需持久化，告诉我。