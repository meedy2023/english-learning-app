# 📁 english_app 文件清单

## 🚀 启动相关（用户主要使用）

| 文件 | 作用 | 使用方式 |
|---|---|---|
| **`start.bat`** | 主启动脚本 | 双击启动 App |
| **`启动App.bat`** | 中文版启动（兼容旧版） | 双击启动 |
| **`停止服务.bat`** | 停止 App 服务 | 双击停止 |
| **`我的IP.bat`** | 查看电脑 IP 地址 | 双击查看，Pad 访问用 |

## 🛠️ 安装与设置（首次使用）

| 文件 | 作用 | 使用方式 |
|---|---|---|
| **`安装依赖.bat`** | 安装 Python 依赖（首次） | 新电脑首次使用 |
| **`自动启动安装.bat`** | 设置开机自启动 | 只需运行一次 |
| **`卸载自启动.bat`** | 取消开机自启动 | 取消自启动 |

## 📖 说明文档

| 文件 | 内容 |
|---|---|
| **`绿色版说明.md`** | 绿色版使用指南（新电脑使用） |
| **`启动说明.md`** | 启动相关说明 |
| **`CLOUD_DEPLOY.md`** | 云部署方案（参考） |
| **`文件清单.md`** | 本文档 |

## 💻 核心代码

| 目录 | 内容 |
|---|---|
| **`backend/`** | FastAPI 后端 |
| └─ `main.py` | API 入口 |
| └─ `words_data.py` | 单词数据库 |
| └─ `textbook3_data.json` | 课文数据（上下册） |
| └─ `ket_data.py` | KET 学习数据 |
| └─ `requirements.txt` | Python 依赖 |
| **`frontend/`** | 前端页面 |
| └─ `index.html` | 主页面 |
| **`start_app.py`** | Python 启动脚本 |

## 🔧 修复/部署脚本（开发用，可忽略）

这些是开发过程中用过的脚本，新电脑使用不需要：

- `fix_*.py` 系列：各种功能修复
- `add_*.py` 系列：功能添加
- `test_*.py` / `test_*.js`：测试脚本
- `check_*.py` / `check_*.js`：检查脚本
- `enable_frontend.py`：前端托管
- `enhance_api.py`：API 增强
- `build_textbook_v2.py`：课文数据生成
- `update_xia_mapping.py`：下册映射
- `diagnose_full.py`：全量诊断
- `extract_js.py`：JS 提取
- `inspect_textbook*.py`：课文检查
- `optimize_textbook_nav.py`：导航优化
- `render.yaml`：Render 部署配置
- `upload_frontend.py`：前端上传
- `start_app.bat` / `启动学习App.bat` / `仅启动后端.bat`：旧启动脚本
- `清理旧文件.py`：旧文件清理
- `slow_down_tts.py`：TTS 语速调整

## 📋 报告/日志（开发记录）

- `FEATURE_UPDATE_LOG.md`
- `FIX_REPORT.md`
- `KET_EXPANDED_REPORT.md`
- `KET_FEATURE_REPORT.md`
- `KET_PAGINATION_REPORT.md`
- `PAGINATION_BUTTON_FIX.md`
- `SMART_TEXTBOOK_NAV_REPORT.md`
- `SPEAK_BTN_FIX_REPORT.md`
- `STARTUP_GUIDE.md`
- `START_GUIDE.md`
- `TEST_GUIDE.md`
- `WORK_COMPLETION_REPORT.md`
- `XIA_TEXTBOOK_SUPPORT_REPORT.md`

---

## 🎯 用户最少需要的文件

如果只想保留核心功能，可以删除"修复/部署脚本"和"报告/日志"，**保留下面这些**就够了：

```
english_app/
├── start.bat              ← 双击启动
├── 停止服务.bat           ← 停止服务
├── 我的IP.bat             ← 看 IP
├── 安装依赖.bat           ← 首次装依赖
├── 自动启动安装.bat       ← 开机自启
├── 卸载自启动.bat         ← 取消自启
├── 绿色版说明.md          ← 使用说明
├── start_app.py           ← 启动脚本
├── backend/
│   ├── main.py
│   ├── words_data.py
│   ├── textbook3_data.json
│   ├── ket_data.py
│   └── requirements.txt
└── frontend/
    └── index.html
```